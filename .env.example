# =============================================================================
# Coupa BYOA / OAF — copy to `.env`, fill in, restart `npm run dev`.
# Only `VITE_*` keys are exposed to the browser.
# All keys read by the app live in `src/features/oaf/viteEnv.js`.
# =============================================================================

VITE_OAF_APP_ID=
VITE_COUPA_DEFAULT_HOST=
VITE_APP_TITLE=
